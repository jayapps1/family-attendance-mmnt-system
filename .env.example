APP_NAME=Family Attendance Management System

DB_HOST=localhost
DB_PORT=5432
DB_NAME=family_db
DB_USER=skilite
DB_PASSWORD=CHANGE_ME

MEDIA_ROOT=media
BACKUP_ROOT=backups

SESSION_TIMEOUT_MINUTES=30
# Generated automatically during first administrator setup; preserve securely.
APP_ENCRYPTION_KEY=

# Optional override if PostgreSQL client tools cannot be found.
# PG_DUMP_PATH=C:/Program Files/PostgreSQL/18/bin/pg_dump.exe

# Google Drive for desktop folder; keep BACKUP_ROOT local.
GOOGLE_DRIVE_BACKUP_DIR=
